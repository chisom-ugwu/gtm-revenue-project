# Workspace Rules & GTM Agent Context

Ensure all agents and scripts respect the instructions defined in [GEMINI.md].
- Only the `default` sales pipeline is inspected.
- Extract pipeline configuration, stages, and win probabilities dynamically via HubSpot's Pipeline API.
